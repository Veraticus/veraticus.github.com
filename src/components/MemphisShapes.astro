---
---

<div class="memphis-shapes" aria-hidden="true">
  <!-- Teal circles - spread out, big one upper right -->
  <div class="memphis-shape circle" style="top: 6%; left: 85%; --size: 56px; --delay: 0s; --duration: 9s;"></div>
  <div class="memphis-shape circle" style="top: 80%; left: 3%; --size: 28px; --delay: -2s; --duration: 7s;"></div>

  <!-- Coral triangles - spread across edges -->
  <div class="memphis-shape triangle" style="top: 15%; left: 5%; --size: 26px; --delay: -1s; --duration: 8s;"></div>
  <div class="memphis-shape triangle" style="top: 88%; left: 48%; --size: 48px; --delay: -3s; --duration: 10s;"></div>

  <!-- Yellow diamonds - corners -->
  <div class="memphis-shape diamond" style="top: 4%; left: 30%; --size: 22px; --delay: -2.5s; --duration: 10s;"></div>
  <div class="memphis-shape diamond" style="top: 70%; left: 8%; --size: 18px; --delay: -6s; --duration: 8s;"></div>

  <!-- Black squiggles - top and left -->
  <div class="memphis-shape squiggle" style="top: 3%; left: 55%; --delay: -1.5s; --duration: 10s;">
    <svg width="80" height="20" viewBox="0 0 80 20" fill="none">
      <path d="M2 10 C12 2, 22 18, 32 10 C42 2, 52 18, 62 10 C72 2, 78 14, 78 10" stroke="#1a1a1a" stroke-width="3" stroke-linecap="round" fill="none"/>
    </svg>
  </div>
  <div class="memphis-shape squiggle" style="top: 50%; left: 2%; --delay: -4s; --duration: 8.5s;">
    <svg width="70" height="18" viewBox="0 0 70 18" fill="none">
      <path d="M2 9 C10 2, 18 16, 26 9 C34 2, 42 16, 50 9 C58 2, 66 14, 68 9" stroke="#1a1a1a" stroke-width="2.5" stroke-linecap="round" fill="none"/>
    </svg>
  </div>

  <!-- Dot clusters - corners -->
  <div class="memphis-shape dots" style="top: 35%; left: 3%; --delay: -3.5s; --duration: 7s;">
    <span class="dot"></span>
    <span class="dot"></span>
    <span class="dot"></span>
    <span class="dot"></span>
  </div>
  <div class="memphis-shape dots" style="top: 92%; left: 75%; --delay: -7s; --duration: 6s;">
    <span class="dot"></span>
    <span class="dot"></span>
    <span class="dot"></span>
    <span class="dot"></span>
  </div>

  <!-- Pink halftone rectangles - spread out -->
  <div class="memphis-shape halftone-rect" style="top: 25%; left: 12%; --delay: -5.5s; --duration: 11s;"></div>
  <div class="memphis-shape halftone-rect" style="top: 93%; left: 30%; --delay: -8s; --duration: 9s;"></div>
</div>

<style>
  .memphis-shapes {
    position: absolute;
    inset: 0;
    z-index: 2;
    pointer-events: none;
    overflow: hidden;
  }

  .memphis-shape {
    position: absolute;
    pointer-events: none;
    animation: memphis-float var(--duration, 7s) ease-in-out infinite;
    animation-delay: var(--delay, 0s);
  }

  /* --- Teal circles --- */
  .circle {
    width: var(--size);
    height: var(--size);
    border-radius: 50%;
    background: #2ec4b6;
    border: 2.5px solid #1a1a1a;
  }

  /* --- Coral triangles --- */
  .triangle {
    width: 0;
    height: 0;
    border-left: calc(var(--size) / 2) solid transparent;
    border-right: calc(var(--size) / 2) solid transparent;
    border-bottom: var(--size) solid #ff6b6b;
    animation: memphis-float var(--duration, 7s) ease-in-out infinite,
               memphis-spin var(--duration, 7s) linear infinite;
    animation-delay: var(--delay, 0s);
  }

  /* --- Yellow diamonds --- */
  .diamond {
    width: var(--size);
    height: var(--size);
    background: #ffe600;
    border: 2.5px solid #1a1a1a;
    transform: rotate(45deg);
    animation: memphis-float var(--duration, 7s) ease-in-out infinite,
               memphis-wiggle var(--duration, 7s) ease-in-out infinite;
    animation-delay: var(--delay, 0s);
  }

  /* --- Black squiggles --- */
  .squiggle {
    opacity: 0.7;
    animation: memphis-float var(--duration, 7s) ease-in-out infinite,
               memphis-wiggle calc(var(--duration, 7s) * 0.8) ease-in-out infinite;
    animation-delay: var(--delay, 0s);
  }

  /* --- Dot clusters --- */
  .dots {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4px;
  }

  .dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: #1a1a1a;
    display: block;
  }

  /* --- Pink halftone rectangles --- */
  .halftone-rect {
    width: 40px;
    height: 24px;
    background: #ff6b6b;
    border: 2px solid #1a1a1a;
    opacity: 0.6;
    background-image: radial-gradient(circle, #1a1a1a 1px, transparent 1px);
    background-size: 5px 5px;
  }

  /* --- Animations --- */
  @keyframes memphis-float {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-18px);
    }
  }

  @keyframes memphis-spin {
    0% {
      transform: rotate(0deg);
    }
    100% {
      transform: rotate(360deg);
    }
  }

  @keyframes memphis-wiggle {
    0%, 100% {
      transform: rotate(45deg) translateX(0);
    }
    25% {
      transform: rotate(50deg) translateX(4px);
    }
    75% {
      transform: rotate(40deg) translateX(-4px);
    }
  }

  /* Reduce motion */
  @media (prefers-reduced-motion: reduce) {
    .memphis-shape {
      animation: none;
    }
  }
</style>
